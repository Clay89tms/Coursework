spring:
  application:
    name: MyCoursework
  datasource:
    url: jdbc:postgresql://localhost:5432/coursework
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
