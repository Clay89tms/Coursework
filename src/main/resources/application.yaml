spring:
  application:
    name: MyCoursework
  datasource:
    url: jdbc:postgresql://localhost:5432/coursework
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

server:
  port: 8081
